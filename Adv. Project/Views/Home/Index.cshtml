@model Adv._Project.Models.DashboardViewModel

@if (Model != null)
{ 
    @if (User?.Identity?.IsAuthenticated == true)
    {
        if (User.IsInRole("PremiumUser"))
        {
            <li class="nav-item" style="list-style: none;">
                <a class="nav-link premium-button"
                   asp-controller="PremiumFeatures"
                   asp-action="Index">
                    Premium Features
                </a>
            </li>
        }
    }
}






<div class="container-fluid px-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0 text-gray-800">Financial Dashboard</h1>
        <div class="btn-group" role="group">
            <a href="?timeRange=ThisMonth&currency=@ViewBag.SelectedCurrency" class="btn time-filter-btn">This Month</a>
            <a href="?timeRange=ThisYear&currency=@ViewBag.SelectedCurrency" class="btn time-filter-btn">This Year</a>
        </div>
    </div>
</div>

<form method="get" class="d-flex align-items-center mb-4">
    <input type="hidden" name="timeRange" value="@ViewContext.HttpContext.Request.Query["timeRange"]" />
    <label for="currency" class="form-label me-2 mb-0">Currency:</label>
    <select name="currency" id="currency" class="form-select me-2 w-auto">
        @foreach (var currency in new[] { "USD", "EUR", "GBP", "JPY", "LBP"})
        {
            if (ViewBag.SelectedCurrency == currency)
            {
                <option value="@currency" selected>@currency</option>
            }
            else
            {
                <option value="@currency">@currency</option>
            }
        }
    </select>
    <button type="submit" class="btn btn-switch">Switch</button>
</form>

<a href="/Transactions/Create" class="btn btn-add-transaction mb-4">Add Transaction</a>


<div class="row">
    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-primary shadow h-100 py-2">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">Total Income</div>
                        <div class="h5 mb-0 font-weight-bold text-gray-800">
                            @ViewBag.SelectedCurrency @Model.TotalIncome.ToString("N2")
                        </div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-dollar-sign fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-danger shadow h-100 py-2">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-danger text-uppercase mb-1">Total Expenses</div>
                        <div class="h5 mb-0 font-weight-bold text-gray-800">
                            @ViewBag.SelectedCurrency @Model.TotalExpenses.ToString("N2")
                        </div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-chart-line fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-success shadow h-100 py-2">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-success text-uppercase mb-1">Savings Rate</div>
                        <div class="h5 mb-0 font-weight-bold text-gray-800">
                            @Model.SavingsRate%
                        </div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-piggy-bank fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-warning shadow h-100 py-2">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">Net Worth</div>
                        <div class="h5 mb-0 font-weight-bold text-gray-800">
                            @ViewBag.SelectedCurrency @((Model.TotalIncome - Model.TotalExpenses).ToString("N2"))
                        </div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-wallet fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-xl-8 col-lg-7">
        <div class="card shadow mb-4">
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                <h6 class="m-0 font-weight-bold text-primary">Monthly Financial Overview</h6>
                <div class="dropdown no-arrow">
                    <a href="#" class="dropdown-toggle" role="button" data-toggle="dropdown">
                        <i class="fas fa-ellipsis-v fa-sm fa-fw text-gray-400"></i>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right shadow">
                        <div class="dropdown-header">Actions:</div>
                        <a href="#" class="dropdown-item">Export</a>
                    </div>
                </div>
            </div>
            <div class="card-body">
                <div class="chart-area">
                    <canvas id="monthlyFinancialsChart"></canvas>
                </div>
            </div>
        </div>
    </div>

    <div class="col-xl-4 col-lg-5">
        <div class="card shadow mb-4">
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                <h6 class="m-0 font-weight-bold text-primary">Expense Categories</h6>
            </div>
            <div class="card-body">
                <div class="chart-pie pt-4 pb-2">
                    <canvas id="expenseCategoriesChart"></canvas>
                </div>
                <div class="mt-4 text-center small">
                    @foreach (var category in Model.ExpensesByCategory)
                    {
                        <span class="mr-2">
                            <i class="fas fa-circle text-primary"></i> @category.Category
                        </span>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="card shadow mb-4">
            <div class="card-header py-3 d-flex justify-content-between align-items-center">
                <h6 class="m-0 font-weight-bold text-primary">Recent Transactions</h6>
                <a href="/Transactions" class="btn btn-primary btn-sm">Details</a>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-bordered" width="100%" cellspacing="0">
                        <thead>
                            <tr>
                                <th>Date</th>
                                <th>Description</th>
                                <th>Category</th>
                                <th>Amount</th>
                                <th>Type</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var transaction in Model.RecentTransactions)
                            {
                                <tr>
                                    <td>@transaction.Date.ToShortDateString()</td>
                                    <td>@transaction.Description</td>
                                    <td>@transaction.Category</td>
                                    <td class="@(transaction.Type == TransactionType.Expense ? "text-danger" : "text-success")">
                                        @ViewBag.SelectedCurrency @transaction.Amount.ToString("N2")
                                    </td>
                                    <td>@transaction.Type</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        // Monthly Financials Chart
        var monthlyFinancialsCtx = document.getElementById('monthlyFinancialsChart').getContext('2d');
        var monthlyFinancialsChart = new Chart(monthlyFinancialsCtx, {
            type: 'bar',
            data: {
                labels: [@Html.Raw(string.Join(",", Model.MonthlyFinancials.Select(m => $"'{m.Month}'").ToList()))],
                datasets: [
                    {
                        label: 'Income',
                        data: [@string.Join(",", Model.MonthlyFinancials.Select(m => m.Income))],
                        backgroundColor: 'rgba(54, 162, 235, 0.5)'
                    },
                    {
                        label: 'Expenses',
                        data: [@string.Join(",", Model.MonthlyFinancials.Select(m => m.Expenses))],
                        backgroundColor: 'rgba(255, 99, 132, 0.5)'
                    }
                ]
            },
            options: {
                responsive: true,
                scales: {
                    y: {
                        beginAtZero: true
                    }
                }
            }
        });

        // Expense Categories Pie Chart
        var expenseCategoriesCtx = document.getElementById('expenseCategoriesChart').getContext('2d');
        var categoryLabels = [@Html.Raw(string.Join(",", Model.ExpensesByCategory.Select(c => $"'{c.Category}'").ToList()))];
        var categoryData = [@string.Join(",", Model.ExpensesByCategory.Select(c => c.Percentage))];

        // Generate dynamic background colors
        var dynamicColors = [
            'rgba(54, 162, 235, 0.8)', // Blue
            'rgba(255, 99, 132, 0.8)', // Red
            'rgba(255, 206, 86, 0.8)', // Yellow
            'rgba(75, 192, 192, 0.8)', // Green
            'rgba(153, 102, 255, 0.8)', // Purple
            'rgba(255, 159, 64, 0.8)'  // Orange
        ];

        // Ensure unique colors based on the number of categories
        var backgroundColors = categoryLabels.map(function (_, index) {
            return dynamicColors[index % dynamicColors.length];
        });

        var expenseCategoriesChart = new Chart(expenseCategoriesCtx, {
            type: 'pie',
            data: {
                labels: categoryLabels,
                datasets: [{
                    data: categoryData,
                    backgroundColor: backgroundColors,
                    hoverBackgroundColor: backgroundColors.map(function (color) {
                        return color.replace('0.8', '1'); // Slightly brighter on hover
                    })
                }]
            },
            options: {
                responsive: true,
                legend: {
                    display: false
                },
                tooltips: {
                    callbacks: {
                        label: function (tooltipItem, data) {
                            var dataset = data.datasets[tooltipItem.datasetIndex];
                            var total = dataset.data.reduce(function (previousValue, currentValue) {
                                return previousValue + currentValue;
                            });
                            var currentValue = dataset.data[tooltipItem.index];
                            var percentage = Math.floor(((currentValue / total) * 100) + 0.5);
                            return `${data.labels[tooltipItem.index]}: ${percentage}%`;
                        }
                    }
                }
            }
        });
    </script>
}
